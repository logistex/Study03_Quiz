# 상식 퀴즈 웹 앱 PRD

- 작성일: 2026-09-19
- 상태: 사용자 검토 대기

## 1. 개요

### 1.1 누가
대학 1학년 학생이 혼자 상식을 공부하며 풀어 봅니다.

### 1.2 무엇을
4지선다 상식 퀴즈 웹 앱입니다.
- 카테고리 4개: 한국사, 세계지리, 과학, 예술과 문화
- 카테고리마다 문항 수가 같습니다. 지금은 카테고리마다 11문제, 총 44문제입니다.

### 1.3 왜
틀린 문제를 그 자리에서 알고 넘어가게 하려고 만듭니다. 답을 고르면 곧바로 정답 여부와 한 줄 해설을 보여 줍니다.

### 1.4 언제
- 한 판은 카테고리 1개의 문항 전부입니다.
- 한 판이 끝나면 점수를 보여 줍니다.

### 1.5 어디서
- 브라우저에서 `index.html`을 직접 열면(`file://`) 동작합니다. 서버는 없습니다.
- 파일은 `index.html`, `style.css`, `script.js`, `questions.js` 4개뿐입니다.
- 휴대폰 폭(360px)부터 PC까지 동작합니다.

## 2. 게임 모드

| 모드 | 시간 제한 | 힌트 | 맞힘 | 틀림 | 순위표 |
|---|---|---|---|---|---|
| 연습 | 없음 | 없음 | 1점 | 0점 | 기록하지 않음 |
| 스피드 | 문항마다 15초 | 없음 | 1점 | 0점 | 기록함 |
| 힌트 | 없음 | 문항마다 1번 | 1점, 힌트를 썼으면 0.5점 | 0점 | 기록함 |

### 2.1 연습
- 시간 제한과 힌트가 없고, 맞히면 1점입니다.
- 순위표에 기록하지 않습니다. 시작 화면(2단계부터는 모드 선택 화면)과 결과 화면에 "순위표에 기록되지 않음"을 표시합니다.
- **틀린 문제 다시 풀기**
  - 결과 화면에 틀린 문항이 있으면 [틀린 문제 다시 풀기] 버튼이 나옵니다.
  - 틀린 문항만 순서와 보기를 다시 섞어서 냅니다. 화면 상단에 "다시 풀기"를 표시합니다.
  - 다시 풀기는 채점하지 않습니다. 끝나면 다시 풀기 결과 화면을 보여 줍니다.
  - 여기서 또 틀린 문항이 있으면 [틀린 문제 다시 풀기]가 다시 나옵니다. 다 맞힐 때까지 반복할 수 있습니다.
  - 그 판의 점수는 처음 한 판의 결과만 인정합니다.

### 2.2 스피드
- 문항마다 15초가 주어지고, 남은 초를 화면에 표시합니다.
- 0초가 되면 오답으로 처리하고, 정답과 해설을 답을 골랐을 때와 똑같이 보여 줍니다.
- 해설이 나오면 타이머가 멈춥니다. [다음]을 누르면 다음 문항에서 다시 15초부터 셉니다.

### 2.3 힌트
- 문항마다 [힌트] 버튼을 한 번 쓸 수 있습니다.
- 누르면 오답 보기 2개를 무작위로 골라 비활성화(흐리게 표시, 선택 불가)합니다. 그 문항에서는 버튼을 다시 쓸 수 없습니다.
- 힌트를 쓰고 맞히면 0.5점, 쓰지 않고 맞히면 1점입니다.
- 점수는 0.5 단위로 표시합니다(예: 7.5 / 10).

### 2.4 공통
- 틀린 문항은 모든 모드에서 0점입니다.
- 판마다 문항 순서와 보기 순서를 섞습니다.
- 게임 도중 다른 화면으로 나가거나 새로 고침하면 그 판은 버립니다. 저장하지 않습니다.

## 3. 화면

`index.html`에 화면마다 `<section>`을 하나씩 미리 두고, 한 번에 하나만 보이게 `hidden`으로 전환합니다.

### 3.1 시작
- 카테고리 버튼 4개가 있습니다.
- 1단계: 카테고리를 고르면 바로 연습 모드로 시작합니다. "순위표에 기록되지 않음"을 표시합니다.
- 2단계부터: 카테고리를 고르면 모드 선택 화면으로 갑니다.
- 3단계부터: [순위표 보기] 버튼이 있습니다.

### 3.2 모드 선택 (2단계부터)
- 연습, 스피드, 힌트 버튼이 있고, 버튼마다 규칙을 한 줄씩 표시합니다.
- 연습 버튼에는 "순위표에 기록되지 않음"을 붙입니다.
- [뒤로]를 누르면 시작 화면으로 돌아갑니다.

### 3.3 문제
- 상단: 카테고리, 모드, 진행(예: 3 / 10), 현재 점수. 스피드 모드는 남은 초도 표시합니다.
- 문제 문장과 보기 버튼 4개가 있습니다. 힌트 모드는 [힌트] 버튼도 있습니다.
- 보기를 고르거나 스피드 모드에서 시간이 끝나면:
  1. 모든 보기와 [힌트] 버튼이 잠깁니다.
  2. 정답 보기는 초록, 고른 오답은 빨강으로 표시합니다. 색만으로 구분하지 않도록 "정답"/"오답" 글자도 함께 표시합니다.
  3. "정답!" 또는 "오답"(시간 초과면 "시간 초과")을 표시합니다.
  4. 한 줄 해설과 출처를 표시합니다. 출처는 기관·문서 이름을 누르면 새 탭에서 URL이 열리는 링크입니다.
  5. [다음] 버튼이 나옵니다. 마지막 문항이면 [결과 보기]가 나옵니다.

### 3.4 결과
- 점수를 "맞힌 수 / 한 판의 문항 수"로 표시합니다.
- 문항별로 문제, 정답/오답 표시, 정답 보기를 목록으로 보여 줍니다.
- 버튼: [같은 모드 다시], [처음으로]
- 연습 모드: "순위표에 기록되지 않음"을 표시합니다. 틀린 문항이 있으면 [틀린 문제 다시 풀기]가 나옵니다(2단계부터).
- 스피드·힌트 모드(3단계부터): 이름 입력칸과 [기록 저장] 버튼이 있고, 저장하면 순위표 화면으로 갑니다.

### 3.5 순위표 (3단계부터)
- 모드 2개(스피드, 힌트) × 카테고리 4개 = 표 8개입니다.
- 표마다 상위 5건을 "순위, 이름, 점수, 날짜(YYYY-MM-DD)"로 보여 줍니다.
- 기록이 없는 표는 "기록 없음"을 표시합니다.
- [처음으로] 버튼이 있습니다.

## 4. 데이터

### 4.1 `questions.js`
일반 `<script>`로 로드하고 전역 상수 `QUESTIONS` 하나를 정의합니다. `file://`에서는 `fetch()`와 ES 모듈이 막히므로 JSON 파일이나 `import`를 쓰지 않습니다.

```js
const QUESTIONS = [
  {
    id: "kh-01",                  // 카테고리 접두어 + 두 자리 번호
    category: "한국사",            // 한국사 | 세계지리 | 과학 | 예술과 문화
    question: "문제 문장",
    choices: ["정답", "오답1", "오답2", "오답3"],
    answer: 0,                    // 작성 시 정답은 항상 0번. 화면에서 섞음
    explanation: "한 줄 해설",
    source: { name: "출처 기관·문서 이름", url: "https://..." }
  }
];
```

- id 접두어: 한국사 `kh`, 세계지리 `wg`, 과학 `sc`, 예술과 문화 `ac`
- `index.html`은 `questions.js`를 `script.js`보다 먼저 로드합니다.

### 4.2 문항 규칙
문항 작성 규칙의 정본은 CLAUDE.md에 있습니다.

1. 해설에는 확인한 출처를 명시합니다(`source.name`과 `source.url`).
2. 해설은 한 줄(대략 60자 이내)입니다.

### 4.3 문항 작성과 검수
- 구현 때 Claude가 40문항을 씁니다.
- 문항마다 실제 출처 페이지를 열어 정답과 해설을 대조한 뒤, 그 페이지의 기관·문서 이름과 URL을 적습니다.
  - 예: 국사편찬위원회 우리역사넷, 브리태니커, 공공기관·국제기구 누리집
- 검수 체크리스트는 문항마다 다음 세 가지입니다.
  - 정답이 하나뿐인가
  - 출처가 적혀 있고 열어서 확인했는가
  - 최상급 표현이면 기준과 시점을 밝혔는가
- 최종 검수는 사용자가 합니다.

### 4.4 순위표 저장 (3단계)
- localStorage 키 하나 `quiz.leaderboard`를 씁니다.
- 값은 `"모드|카테고리"`를 키로 하는 객체입니다.
  ```json
  { "speed|한국사": [ { "name": "민지", "score": 8, "date": "2026-09-19T10:21:00.000Z" } ] }
  ```
  - 모드 키: `speed`, `hint`
  - `date`는 저장 시각(ISO 문자열)입니다. 화면에는 날짜만 표시합니다.
- 정렬: 점수 내림차순, 동점이면 `date` 오름차순(먼저 세운 기록이 위). 상위 5건만 남기고 버립니다.
- 이름: 앞뒤 공백을 지운 뒤 1~10자입니다. 비어 있으면 [기록 저장]을 비활성화합니다. 화면에는 `textContent`로만 넣습니다.
- 읽기와 쓰기는 `try/catch`로 감쌉니다. 저장소를 쓸 수 없거나 값이 깨져 있으면 "기록을 저장할 수 없음"을 표시하고, 게임은 계속 됩니다. 깨진 값은 빈 순위표로 취급합니다.

## 5. `script.js` 구조

위에서 아래로 세 덩어리입니다.

### 5.1 순수 로직
DOM을 건드리지 않아 콘솔에서 바로 확인할 수 있습니다.

| 함수 | 하는 일 |
|---|---|
| `shuffle(array)` | 복사본을 섞어 반환(Fisher–Yates). 원본은 바꾸지 않음 |
| `buildRound(questions, category)` | 해당 카테고리 문항을 섞고, 문항마다 보기를 섞어 새 정답 위치를 계산 |
| `scoreAnswer(isCorrect, usedHint)` | 1, 0.5, 0 중 하나를 반환 |
| `pickHintRemovals(question)` | 지울 오답 보기 번호 2개를 무작위로 반환 |
| `validateQuestions(questions)` | 카테고리마다 같은 문항 수, 보기 4개, 보기 중복 없음, `answer` 범위, 필수 항목, 출처 URL(`https://`), id 중복 없음을 검사하고 문제 목록을 반환 |
| `insertRecord(list, record)` | 4.4의 정렬 규칙으로 넣고 상위 5건만 반환 |

### 5.2 상태
객체 하나로 관리합니다.

```js
const state = {
  category, mode,          // 현재 카테고리와 모드
  round, index,            // 이번 판 문항 목록과 현재 번호
  score, results,          // 점수와 문항별 결과
  usedHint,                // 현재 문항에서 힌트를 썼는지
  timerId, secondsLeft,    // 스피드 모드 타이머
  isRetry                  // 틀린 문제 다시 풀기 중인지
};
```

### 5.3 화면 조작
`showScreen(id)`, `renderQuestion()`, `handleAnswer(choiceIndex)`, `handleTimeout()`, `startTimer()`, `stopTimer()`, `useHint()`, `renderResult()`, `saveRecord()`, `renderLeaderboard()`

- 화면을 떠나거나 새 판을 시작할 때 반드시 `stopTimer()`를 호출합니다. 타이머가 겹쳐 돌지 않게 하기 위해서입니다.

### 5.4 오류 처리
- 페이지를 열 때 `validateQuestions`에서 문제가 나오면 콘솔에 목록을 출력하고, 시작 화면에 "문항 데이터 오류"를 표시하고 카테고리 버튼을 비활성화합니다.

## 6. 구현 단계와 완료 기준

### 1단계: 연습 모드와 점수
모드 선택 화면과 틀린 문제 다시 풀기는 아직 만들지 않습니다.
- [ ] 시작 화면에서 카테고리를 고르면 연습 모드 11문제가 섞여서 나온다.
- [ ] 답을 고르면 정답 여부, 한 줄 해설, 출처가 바로 나오고 보기가 잠긴다.
- [ ] 결과 화면에 "n / 10"과 "순위표에 기록되지 않음"이 나온다. 시작 화면에도 같은 문구가 있다.
- [ ] `questions.js`에 40문항이 모두 있고 `validateQuestions`를 통과한다.
- [ ] 40문항 모두 4.3의 검수 체크리스트를 통과한다.

### 2단계: 스피드, 힌트, 모드 선택, 틀린 문제 다시 풀기
- [ ] 카테고리를 고르면 모드 선택 화면이 나온다.
- [ ] 스피드: 15초가 지나면 오답 처리되고 정답과 해설이 나온다.
- [ ] 스피드: 해설이 나온 동안 타이머가 멈추고, [다음]을 누르면 15초부터 다시 센다.
- [ ] 힌트: 문항마다 한 번만 쓸 수 있고, 오답 2개가 비활성화된다. 힌트를 쓰고 맞히면 0.5점이다.
- [ ] 연습: 틀린 문제만 다시 풀 수 있고, 다 맞힐 때까지 반복할 수 있다. 점수는 바뀌지 않는다.

### 3단계: 점수 저장과 순위표
- [ ] 스피드·힌트 모드 결과를 이름과 함께 저장한다.
- [ ] 순위표에 모드 × 카테고리별 상위 5건이 정렬 규칙대로 나온다.
- [ ] 새로 고침한 뒤에도 기록이 남아 있다.
- [ ] 저장소를 쓸 수 없으면 안내 문구가 나오고 게임은 계속 된다.

## 7. 검증 방법

빌드 도구와 테스트 프레임워크는 쓰지 않습니다.

- **순수 로직:** 주소 끝에 `?test`를 붙여 열면(`index.html?test`) `script.js`가 자체 점검을 실행해 콘솔에 통과/실패를 출력합니다. 점검 대상은 섞기, 점수 계산, 힌트 선택, 문항 검사, 순위 정렬입니다. 별도 파일을 만들지 않으므로 4개 파일 조건을 지킵니다.
- **화면:** 단계마다 브라우저에서 `file://`로 직접 열어 확인합니다. 모드마다 한 판씩 끝까지 풀어 보고, 휴대폰 폭(360px)에서도 확인합니다.
- **문항 내용:** 4.3의 검수 체크리스트를 따릅니다.

## 8. 범위 밖
- 사용자 계정, 서버, 온라인 순위표
- 문항 편집 화면
- 효과음, 애니메이션 효과
- 다국어 지원
- 난이도 구분
- 진행 중인 판 이어 하기
